const portfolio =(name) =>{
    var ptf =()=>{
        ptf:name = `${name}`
    }
}
const addStock = (ptf, stock)=>{
    var stocks = ptf.stocks;
    if(stocks){
        //add stock to list.
        
    }else{
        ptf.stocks = `{${stock}}`
    }
}